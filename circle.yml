machine:
  environment:
    PYTHONPATH: .
  python:
    version: 3.5.2
  services:
    - redis

dependencies:
  override:
    - pip install -r requirements.txt

test:
  pre:
    - pip install -r requirements_tests.txt
    - flake8
  override:
    - pytest
